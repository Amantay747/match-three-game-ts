# Развертывание на Railway

## Подготовка

1. Убедитесь, что у вас установлены:
   - Node.js 18+
   - PostgreSQL (может быть управляемая база на Railway)

2. Установите Railway CLI:
   ```bash
   npm install -g @railway/cli
   ```

## Развертывание

### Способ 1: Через Railway Dashboard (Рекомендуется)

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub"
3. Выберите репозиторий `match-three-game-ts`
4. Railway автоматически обнаружит `package.json`
5. Добавьте PostgreSQL базу:
   - Нажмите "+ Add Services" → "PostgreSQL"
6. Railway автоматически создаст переменную `DATABASE_URL`

### Способ 2: Через Command Line

```bash
# Войдите в Railway
railway login

# Создайте новый проект
railway init

# Добавьте PostgreSQL
railway add

# Разверните приложение
railway up
```

## Окружение на Railway

Railway автоматически установит следующие переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `NODE_ENV` - установите в `production`
- `PORT` - будет установлен Railway (обычно 3000)

## Проверка развертывания

После развертывания:

```bash
# Просмотр логов
railway logs

# Проверка статуса
railway status

# Открыть в браузере
railway open
```

## Переменные окружения на Railway

Если нужно установить дополнительные переменные, перейдите в:
**Project Settings → Variables** и добавьте:

```
NODE_ENV=production
API_BASE_URL=https://your-railway-url.railway.app
```

## Отладка

Если есть проблемы с подключением:

1. Проверьте, что PostgreSQL сервис подключен к приложению
2. Убедитесь, что `DATABASE_URL` установлена корректно
3. Посмотрите логи: `railway logs`

## Миграции базы данных

Если используются миграции:

```bash
# Подключитесь к базе на Railway
railway connect postgres

# Или запустите миграции перед стартом в railway.json
```

## Мониторинг

Railway предоставляет встроенный мониторинг:
- Логи приложения
- Метрики ресурсов (CPU, RAM)
- История развертываний

Приложение готово к развертыванию на Railway! 🚀
