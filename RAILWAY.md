# Развертывание на Railway

## Подготовка

1. Убедитесь, что у вас установлены:
   - Node.js 18+
   - PostgreSQL (может быть управляемая база на Railway)

2. Установите Railway CLI (опционально):
   ```bash
   npm install -g @railway/cli
   ```

## Развертывание на Railway

### Способ 1: Через Railway Dashboard (Рекомендуется)

1. **Создать проект:**
   - Зайдите на [railway.app](https://railway.app)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub"
   - Выберите репозиторий `match-three-game-ts`
   - Railway автоматически обнаружит конфигурацию

2. **Добавить PostgreSQL:**
   - Нажмите "+ New Service"
   - Выберите "PostgreSQL"
   - Railway автоматически создаст переменные окружения для подключения

3. **Переменные окружения:**
   - Перейдите в **Project Settings → Variables** и установите:
   ```
   NODE_ENV=production
   ```
   - Railway автоматически создаст переменные для PostgreSQL:
     - `DATABASE_URL` (если использовать встроенный PostgreSQL)
     - или отдельные переменные: `PGHOST`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`, `PGPORT`

4. **Развернуть:**
   - Railway автоматически развернет приложение после push в GitHub
   - Приложение будет доступно по адресу, указанному в Railway

### Способ 2: Через Command Line (если установлена Railway CLI)

```bash
# Войти в Railway
railway login

# Инициализировать новый проект
railway init

# Добавить PostgreSQL
railway add

# Установить переменные окружения
railway variables

# Развернуть
railway up
```

## Проверка переменных окружения на Railway

После добавления PostgreSQL, приложение будет получать одно из:

**Вариант 1 - DATABASE_URL (встроенный PostgreSQL):**
```
DATABASE_URL=postgresql://user:password@host:port/database
```

**Вариант 2 - Отдельные переменные (стандарт PostgreSQL):**
```
PGHOST=host
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=database
```

Приложение автоматически использует оба варианта.

## Логирование подключения

При запуске приложение выведет:
```
==================================================
Database Configuration
==================================================
DATABASE_URL: ✓ Set или ✗ Not set
DB_HOST: (значение или не установлена)
DB_NAME: (значение или не установлена)
NODE_ENV: production
==================================================
✓ Using DATABASE_URL for database connection
или
✓ Using individual environment variables for database connection
Connecting to: user@host:port/database
==================================================
```

## Отладка при ошибке подключения

Если приложение не может подключиться:

1. **Проверьте логи:**
   ```bash
   railway logs
   ```

2. **Убедитесь, что PostgreSQL сервис подключен:**
   - В Railway Dashboard перейдите в "Services"
   - Убедитесь, что есть сервис PostgreSQL
   - Он должен быть в одной сети с приложением

3. **Проверьте переменные окружения:**
   - Railway Dashboard → Variables
   - Убедитесь, что есть одна из конфигураций подключения

4. **Проверьте конфигурацию базы:**
   - Если используется внешняя база PostgreSQL, убедитесь что:
     - Хост доступен из интернета
     - Порт открыт
     - Учетные данные верные

## Миграции базы данных

Если используются миграции, добавьте в `railway.json`:

```json
{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "npm run migrate && npm start",
    "restartPolicyMaxRetries": 5
  }
}
```

## Мониторинг и логирование

Railway предоставляет встроенные инструменты:
- **Логи:** Railway Dashboard → Logs
- **Метрики:** Railway Dashboard → Metrics (CPU, RAM, Network)
- **История развертываний:** Railway Dashboard → Deployments

## Доступ к приложению

После развертывания приложение будет доступно по URL:
- `https://[ваш-проект]-[подслой]-[id].railway.app`
- API Docs: `https://[url]/api-docs`
- Health check: `https://[url]/health`

## Полезные команды (через CLI)

```bash
# Просмотр логов
railway logs

# Просмотр статуса
railway status

# Открыть приложение в браузере
railway open

# Подключиться к базе данных
railway connect postgres

# Просмотр переменных окружения
railway variables
```

## Перезагрузка приложения

Если нужно перезагрузить приложение на Railway:
- Через Dashboard: нажмите кнопку "Redeploy"
- Через CLI: `railway redeploy`

Приложение готово к развертыванию на Railway! 🚀
